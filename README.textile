h1. Motion Planning with a LIDAR

The software is split into six parts:

h2. LIDAR data loading

bc. @params Real world
@return Point cloud

h2. Discretize Image

bc. @params Point cloud (Bitmap in Simulation)
@return Points

If in test mode, instead of point cloud a ready bitmap is used, i.e. a mock up via Google Maps API

Raytracing, Bresenham

h2. Edge detection

bc. @params Points
@return Pairs of points (start and end of walls)

Linear Regression

h2. Visibility detection

bc. @params Pairs of points (start and end of walls)
@return Borders: Pairs of points describing the visible area

Sweep Line

h2. Path planning

bc. @params Borders
@params Goal
@return Motion: List of intermediate configurations to goal configuration

A*, Car model

h2. Animation

bc. @params Motion

Canvas
